# macOS
.DS_Store
**/.DS_Store

# Editor
.vscode/
.idea/

# Logs
*.log

# Design source export (large binary)
Infinite Material Tech Redesign.zip

# Source PDFs (catalog + full test reports) — only extracted pages ship in web/public/docs
pdf/

# Working notes / content source (design briefs, article drafts + large source images)
PROMPT*.MD
บทความ*/

# วิดีโอต้นฉบับจากกล้อง/ตัดต่อ (HEVC ไฟล์ใหญ่) — ตัวที่เว็บใช้จริงคือไฟล์ที่
# แปลงเป็น H.264 แล้วใน web/public/videos/ ต้นฉบับไม่ต้องเข้า git เพราะไฟล์
# ใหญ่ระดับนี้ลบทีหลังก็ยังค้างใน history ตลอดไป
/*.mp4
/*.mov

# Stitch export ของหน้าจอหลังบ้าน (4 MB, ส่วนใหญ่เป็น PNG)
# ตัว design system ที่ต้องใช้จริงถูกคัดไว้ที่ docs/design/admin-design-system.md แล้ว
stitch_*.zip

# symlink ของ Claude Code ที่ชี้ไป .agents/skills — เป็นของเฉพาะเครื่อง
.claude/skills/
