import "./admin.css";

export const metadata = {
  title: "ระบบจัดการเนื้อหา | Infinite Material",
  robots: { index: false, follow: false },
};

export default function AdminRootLayout({ children }) {
  return children;
}
